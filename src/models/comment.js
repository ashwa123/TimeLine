class Comment {
    constructor(obj) {
        this.category = obj.category
        this.name  = obj.name,
        this.email   = obj.email,
        this.comment  = obj.comment
    }
}

module.exports = Comment;
